# Personal Website

A clean, minimal personal resume site built with HTML and CSS.

## Structure

- `index.html` — main page with name, bio, and sections
- `styles.css` — all styling: layout, typography, spacing

## Running locally

Open `index.html` directly in your browser — no build step needed.
